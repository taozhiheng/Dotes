<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.BLUE_LIGHT">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.BLUE_DARK" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/accent_material_dark</item>
        <!--Status bar color-->
        <!--<item name="colorPrimaryDark">@color/accent_material_light</item>-->
        <!--Window color-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <style name="AppTheme.BLUE_LIGHT" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/accent_material_light</item>
        <!--Status bar color-->
        <!--<item name="colorPrimaryDark">@color/accent_material_light</item>-->
        <!--Window color-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <style name="AppTheme.ORANGE_DARK" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@android:color/holo_orange_dark</item>
        <!--Status bar color-->
        <!--<item name="colorPrimaryDark">@color/accent_material_light</item>-->
        <!--Window color-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <style name="AppTheme.ORANGE_LIGHT" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- Actionbar color -->
        <item name="colorPrimary">@android:color/holo_orange_light</item>
        <!--Status bar color-->
        <!--<item name="colorPrimaryDark">@color/accent_material_light</item>-->
        <!--Window color-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>

    </style>
    <style name="AppTheme.BLANK" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
